---
title: Git
summary: Core commands for status, branching, and review.
color: "#ff9f1c"
---

<SheetGrid>
  <SheetCard title="Status and Diff">
    |  |
    | --- |
    | <SheetCommand title="Inspect working tree" command="git status" description="See branch info, staged files, and unstaged changes." /> |
    | <SheetCommand title="Review unstaged changes" command="git diff" description="Check edits before staging." example="git diff src/components/sheet-command.tsx" /> |
    | <SheetCommand title="Review staged changes" command="git diff --staged" description="Verify exactly what will be committed." example="git diff --staged" /> |
    | <SheetCommand title="Browse compact history" command="git log --oneline --graph" description="Visual overview of recent commits and branches." example="git log --oneline --graph --all" /> |
  </SheetCard>
  <SheetCard title="Keyboard Shortcuts (dummy)">
    | Shortcut | Action |
    | --- | --- |
    | `q` | Quit |
    | `g g` | Go to top |
    | `G` | Go to bottom |
    | `z z` | Center view |
    | `/ <pattern>` | Search |
    | `n` | Next match |
    | `N` | Previous match |
  </SheetCard>
  <SheetCard title="Branching">
    |  |
    | --- |
    | <SheetCommand title="Create and switch branch" command="git switch -c <name>" description="Start work in a dedicated branch." example="git switch -c feat/dark-mode" /> |
    | <SheetCommand title="Switch branch" command="git switch <name>" description="Move to an existing local branch." example="git switch main" /> |
    | <SheetCommand title="Delete merged branch" command="git branch -d <name>" description="Clean up local branches after merge." example="git branch -d feat/dark-mode" /> |
    | <SheetCommand title="Update remotes and prune" command="git fetch --prune" description="Refresh remote state and remove stale refs." /> |
  </SheetCard>
  <SheetCard title="Commit Flow">
    |  |
    | --- |
    | <SheetCommand title="Stage by hunk" command="git add -p" description="Selectively stage only relevant changes." /> |
    | <SheetCommand title="Commit staged changes" command={'git commit -m "message"'} description="Create a commit with an explicit message." example='git commit -m "feat: add dark mode toggle"' /> |
    | <SheetCommand title="Unstage file" command="git restore --staged <file>" description="Remove a file from the staging area." example="git restore --staged src/app/globals.css" /> |
    | <SheetCommand title="Inspect one commit" command="git show <sha>" description="Display diff, metadata, and message for a commit." example="git show a3f9c12" /> |
  </SheetCard>
  <SheetCard title="Extra Commands (dummy)">
    |  |
    | --- |
    | <SheetCommand title="Stash changes" command="git stash" description="Temporarily shelve uncommitted changes." /> |
    | <SheetCommand title="Pop stash" command="git stash pop" description="Reapply the most recent stash." /> |
    | <SheetCommand title="Cherry-pick commit" command="git cherry-pick <sha>" description="Apply a specific commit onto current branch." example="git cherry-pick a3f9c12" /> |
  </SheetCard>
</SheetGrid>

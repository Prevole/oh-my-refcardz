// Public surface for the blocks module (components layer).
//
// Block constraints and resize handles come from the data registry
// (src/lib/layout/blocks). This barrel exposes only the React renderer
// pipeline.

export { BlockRenderer } from "./block-renderer";
